# Legacy path redirects (also handled by _worker.js)
/en.html / 301
/jp.html / 301
/en / 301
/jp / 301

# Duplicate content redirects
/index.html / 301
/index / 301

# 2026-09-10: 단독도메인 랜딩 하이픈 별칭 _redirects 200-rewrite 는 전부 제거함.
#   env.ASSETS 가 이 rewrite 를 적용해 target(.html)로 보내면 그 .html 이 다시
#   언더스코어 pretty-URL 308(→/cotton_print) 을 유발해 실패(Location:/cotton_print 확인).
#   대신 deploy.sh 가 하이픈 파일명 사본(cotton-print.html 등)을 만들고, 워커가 하이픈 pretty-URL 을
#   env.ASSETS.fetch → 사본이 직접 서빙됨(_redirects 불필요, 언더스코어 회피).

# NOTE: HTML page rewrites (/mypage → mypage.html) and SPA fallback
# are NOT listed here because _worker.js handles all routing.
# Having them here causes 308 redirect loops with Cloudflare Pretty URLs.
